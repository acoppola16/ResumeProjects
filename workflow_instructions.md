# Workflow Instructions

This document provides guidelines for using the project workflow.

## Prerequisites

Before running the workflow, ensure that required dependencies are installed:

```bash
pip install opencv-python
```

